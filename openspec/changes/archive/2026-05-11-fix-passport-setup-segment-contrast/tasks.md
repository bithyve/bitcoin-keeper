## 1. UI Components

- [x] 1.1 Update `src/components/SegmentController.tsx` so inactive segment labels use explicit themed text color for readability.
- [x] 1.2 Keep selected segment label styling unchanged and verify Singlesig/Multisig labels remain readable.

## 2. Business Logic / Hooks

- [x] 2.1 Confirm no changes are required to signer setup flow logic or hooks; keep behavior unchanged.

## 3. Store (Slice + Saga)

- [x] 3.1 Confirm no Redux slice/saga updates are needed for this UI-only fix.

## 4. Storage

- [x] 4.1 Confirm no Realm/MMKV schema or persisted state changes are needed.

## 5. Tests

- [x] 5.1 Run targeted existing validation for changed UI file(s) and ensure no regressions in the touched area.
