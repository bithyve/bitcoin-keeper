import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  Text,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
        <Text>Hexa Keepr</Text>    
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
});

export default App;
