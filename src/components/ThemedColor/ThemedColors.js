import Colors from 'src/theme/Colors';

const themeColors = {
  footer_selected_option: {
    DARK: Colors.headerWhite,
    LIGHT: Colors.primaryGreen,
    PRIVATE: Colors.goldenGradient,
    PRIVATE_LIGHT: Colors.goldenGradient,
  },
};

export default themeColors;
