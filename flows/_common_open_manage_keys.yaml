appId: ${APPID}
---
- runFlow: _common_unlock_if_needed.yaml
- runFlow:
    when:
      visible:
        id: "btn_Manage\nKeys"
    commands:
      - tapOn:
          id: "btn_Manage\nKeys"
- runFlow:
    when:
      notVisible:
        id: "btn_Manage\nKeys"
    commands:
      - tapOn: "Manage Keys"
- extendedWaitUntil:
    visible: "Manage Keys"
    timeout: 15000
